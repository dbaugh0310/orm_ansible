---
# This always runs for every server
- name: Run basic ping check
  include_tasks: ping.yml

# This only runs if the host is in the 'web_servers' group
- name: Run webserver specific checks
  include_tasks: webserver.yml
  when: "'role_webserver' in group_names"